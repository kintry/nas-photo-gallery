# 局域网NAS相册系统 — 技术设计文档

## 1. 系统架构

```
┌─────────────────────────────────────────────────┐
│                 手机/平板/电脑                    │
│           浏览器访问 http://192.168.2.119:5000    │
└──────────────────────┬──────────────────────────┘
                       │ HTTP (局域网)
┌──────────────────────▼──────────────────────────┐
│         WSL (HP电脑) — Flask Web 服务             │
│    ┌──────────┬──────────┬──────────────────┐   │
│    │ 首页路由  │ 大图路由  │ 缩略图生成       │   │
│    │ /        │ /view    │ 缓存             │   │
│    └──────────┴──────────┴──────────────────┘   │
│    ┌──────────┬──────────┬──────────────────┐   │
│    │ 点赞API   │ 图片API  │ 相册扫描+缓存    │   │
│    └──────────┴──────────┴──────────────────┘   │
└──────────────┬──────────────────┬───────────────┘
               │ SSH/SFTP          │ 读取/生成
               ▼                    ▼
┌──────────────────────┐  ┌───────────────────┐
│   NAS (armbian)      │  │ WSL本地缓存       │
│   /media/devmon/     │  │ cache/thumbs/     │
│   SNAKE1, SNAKE2,    │  │ cache/index.json  │
│   OECT-HOME/相册     │  │                   │
└──────────────────────┘  └───────────────────┘
```

## 2. 路由设计

### 2.1 页面路由

| 路由 | 方法 | 说明 |
|------|------|------|
| `GET /` | 首页 | 相册列表+照片网格 |
| `GET /album/<path>` | 相册浏览 | 某个目录下的照片网格 |
| `GET /view` | 大图浏览 | 全屏大图模式（参数: `?img=xxx&album=xxx`） |

### 2.2 API路由

| 路由 | 方法 | 说明 |
|------|------|------|
| `GET /api/photos` | JSON | 获取照片列表（分页，参数: `album`, `page`, `per_page`） |
| `GET /api/albums` | JSON | 获取相册目录树 |
| `GET /api/photo/<photo_id>` | JSON | 单张照片信息（文件名、大小、日期等） |
| `GET /thumb/<photo_id>` | 图片 | 缩略图（400px宽） |
| `GET /raw/<photo_id>` | 图片 | 原图 |
| `POST /api/like` | JSON | 点赞/取消点赞 `{"photo_id": "xxx"}` |
| `GET /api/likes` | JSON | 获取已点赞列表 |

## 3. 前端设计

### 3.1 技术选型

- **纯HTML + CSS + JavaScript**（单页应用）
- 不使用任何前端框架，减少依赖
- 使用 `fetch` API 调用后端接口
- 使用 `CSS Grid` 实现相册网格布局
- 使用 `touch` 事件实现手机滑动切换

### 3.2 页面结构

```
┌─────────────────────────────────────────┐
│  顶部导航栏                              │
│  [相册名]          [自动播放] [搜索]     │
├─────────────────────────────────────────┤
│                                         │
│  ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐         │
│  │   │ │   │ │   │ │   │ │   │         │
│  │   │ │   │ │   │ │   │ │   │         │
│  └───┘ └───┘ └───┘ └───┘ └───┘         │
│  ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐         │
│  │   │ │   │ │   │ │   │ │   │         │
│  │   │ │   │ │   │ │   │ │   │         │
│  └───┘ └───┘ └───┘ └───┘ └───┘         │
│                                         │
│         [加载更多...]                     │
│                                         │
├─────────────────────────────────────────┤
│  底部                                    │
└─────────────────────────────────────────┘
```

### 3.3 大图浏览模式

```
┌─────────────────────────────────────────┐
│  [← 返回]           61 / 1566  [信息]   │
├─────────────────────────────────────────┤
│                                         │
│                                         │
│          ←      [大图]       →          │
│                                         │
│                                         │
├─────────────────────────────────────────┤
│  [下载] [❤] [自动播放] [打印] [收起列表] │
│  ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐ ┌─┐     │
│  │ │ │ │ │ │ │ │ │ │ │ │ │ │ │ │     │
│  └─┘ └─┘ └─┘ └─┘ └─┘ └─┘ └─┘ └─┘     │
│  ← 缩略图滚动条 →                       │
└─────────────────────────────────────────┘
```

## 4. 图片处理

### 4.1 缩略图生成

- 使用 Pillow 生成缩略图
- 缓存位置：`~/.hermes/scripts/nas_photo_gallery/cache/thumbs/`
- 缩略图大小：400px宽（高度自适应）
- 首次访问生成，后续直接返回缓存

### 4.2 原图服务

- 直接通过 Flask 的 `send_file` 发送原图
- 带 `Range` 请求头的支持（支持图片分段加载）

## 5. 目录扫描

### 5.1 扫描策略

- 启动时扫描配置的照片根目录
- 递归扫描所有 `*.jpg`, `*.jpeg`, `*.png` 文件
- 建立文件索引缓存（JSON格式，存储在 `~/.hermes/scripts/nas_photo_gallery/cache/`）
- 支持手动刷新索引

### 5.2 索引结构

```json
{
  "albums": [
    {
      "name": "庄润秋的影像",
      "path": "/media/devmon/SNAKE2/庄润秋的影像",
      "cover": "thumb_hash",
      "photo_count": 1566,
      "date_range": "2018-05 ~ 2026-07"
    }
  ],
  "photos": {
    "file_hash_md5": {
      "filename": "IMG_20260701_123355.jpg",
      "path": "/media/devmon/SNAKE2/庄润秋的影像/IMG_20260701_123355.jpg",
      "album": "庄润秋的影像",
      "size": 4523021,
      "width": 4032,
      "height": 3024,
      "date": "2026-07-01 12:33:55"
    }
  }
}
```

## 6. 部署

### 6.1 启动方式

```bash
# 安装依赖
pip3 install flask pillow

# 启动（后台运行）
nohup python3 /home/kintry/.hermes/scripts/nas_photo_gallery/app.py \
  --port 5000 \
  --photo-dirs /media/devmon/SNAKE2,/media/devmon/OECT-HOME/家庭影像,/media/devmon/SNAKE1 \
  > /tmp/photo_gallery.log 2>&1 &

# 或使用 systemd
```

### 6.2 访问方式

- 局域网内：`http://192.168.2.104:5000`
- 如果NAS和WSL互通：`http://192.168.2.104:5000`

### 6.3 开机自启

通过 CasaOS GUI 或 `/etc/rc.local` 添加启动命令。

## 7. 配色方案

参考拍立享的深色主题：

```
--bg-primary: #1a1a2e      // 深色背景
--bg-secondary: #16213e    // 次级背景
--bg-card: #f8f9fa         // 卡片背景（浅色模式）
--accent: #3498db          // 主色调（蓝色）
--text-primary: #ffffff    // 主文字（深色模式下）
--text-secondary: #95a5a6  // 次级文字
--border: #2c3e50         // 边框
--hover: #2980b9          // 悬停高亮
```

## 8. 文件结构

```
nas_photo_gallery/
├── app.py                 # Flask 主程序
├── templates/
│   ├── index.html         # 首页（相册列表+网格）
│   └── viewer.html        # 大图浏览
├── static/
│   ├── css/
│   │   └── style.css      # 样式
│   └── js/
│       └── app.js         # 前端交互
├── cache/
│   ├── thumbs/            # 缩略图缓存
│   └── index.json         # 文件索引
├── requirements.txt
├── 需求说明书.md
└── 技术设计文档.md
```

## 9. 开发计划

| 阶段 | 内容 | 预估工时 |
|------|------|---------|
| Phase 1 | Flask服务搭建 + 目录扫描 + 缩略图生成 | 2h |
| Phase 2 | 首页网格 + 分页加载 | 1.5h |
| Phase 3 | 大图浏览模式 + 切换 + 缩放 | 2h |
| Phase 4 | 底部工具栏（下载、点赞、自动播放、信息） | 2h |
| Phase 5 | 相册目录选择 + 搜索 | 1h |
| Phase 6 | 部署 + 测试 + 调优 | 1h |
