# 局域网NAS相册系统 — 需求说明书

## 1. 项目背景

用户家中有NAS（armbian + CasaOS），存放了大量家庭照片和影像资料。需要一个方便在局域网内通过手机/平板/电脑访问的相册系统，能够在微信中直接打开使用，无需安装App。

## 2. 数据源

照片存储在NAS的以下路径（exfat格式硬盘）：

| 硬盘 | 路径 | 内容 |
|------|------|------|
| SNAKE2 | `/media/devmon/SNAKE2/叶丽芳的图片/` | 按年月分类（2018~至今） |
| SNAKE2 | `/media/devmon/SNAKE2/庄润晨的影像/` | 妹妹的影像 |
| SNAKE2 | `/media/devmon/SNAKE2/庄润秋的影像/` | 姐姐的影像 |
| OECT-HOME | `/media/devmon/OECT-HOME/相册/` | 通用相册 |
| OECT-HOME | `/media/devmon/OECT-HOME/家庭影像/` | 家庭照片 |
| SNAKE1 | `/media/devmon/SNAKE1/` | 即时照片（如手机备份） |

## 3. 目标用户

- 家庭成员，在局域网（家庭WiFi）内使用
- 主要使用场景：手机（微信内打开）、平板、电脑浏览器
- 非技术用户，界面需要简洁直观

## 4. 功能需求

### 4.1 相册首页（照片网格）

| 需求 | 说明 |
|------|------|
| 照片列表 | 以网格形式展示所有照片，默认按文件时间倒序 |
| 分页/懒加载 | 滚动到底部自动加载下一页，每次20张 |
| 缩略图 | 服务端生成缩略图（400px宽），减少手机流量 |
| 响应式 | PC端5列、平板4列、手机2列 |

### 4.2 大图浏览（照片详情）

以拍立享（pailixiang.com）为参考：

| 需求 | 说明 |
|------|------|
| 全屏大图 | 点击缩略图进入全屏大图浏览模式，黑色背景 |
| 放大缩小 | 支持双指缩放 + 双击放大/缩小（H5自带手势） |
| 左右切换 | 左右箭头切换上一张/下一张，支持滑动 |
| 图片计数 | 顶部显示当前图片位置，如 `23 / 1566` |
| 顶部相册名 | 显示当前相册名称 |

### 4.3 底部工具栏

| 功能 | 说明 |
|------|------|
| 缩略图列表 | 底部显示横向滚动缩略图列表，可收起/展开 |
| 下载按钮 | 将当前大图下载到设备 |
| 点赞按钮 | ❤️ 点赞，用图标变化表示已赞（数据存SQLite） |
| 照片信息 | 显示文件名、拍摄时间、分辨率、文件大小 |
| 自动播放 | 自动轮播，间隔3秒，可暂停 |
| 打印按钮 | 调起浏览器打印功能或保存为PDF |

### 4.4 相册管理（可选优先）

| 功能 | 说明 |
|------|------|
| 相册列表 | 按目录展示不同相册（如"2018年5月"、"庄润秋的影像"） |
| 目录浏览 | 用户可以从相册目录进入，而不是所有照片混在一起 |
| 搜索 | 按文件名/日期搜索照片 |

## 5. 非功能需求

| 需求 | 说明 |
|------|------|
| 架构 | 纯局域网，不依赖外网 |
| 访问方式 | 手机/电脑浏览器直接打开 `http://192.168.2.104:5000` |
| 响应速度 | 首屏加载 < 2秒，翻页 < 1秒 |
| 并发 | 支持3-5个家庭成员同时浏览 |
| 存储 | 照片存在NAS硬盘上，服务端不复制/不搬迁 |
| 后端语言 | Python 3.12（NAS已有） |
| 依赖安装 | pip install flask pillow |

## 6. 约束

- NAS CPU：RK35xx 架构（aarch64），内存 4GB
- 存储空间：系统盘5.3G（很小），照片在exfat外接硬盘
- 无Docker容器运行（CasaOS已装但未配置）
- 需安装Python包：`flask`, `pillow`, `jinja2`

## 7. 数据模型

```json
// 点赞数据 ~/.hermes/scripts/likes.json
{
  "照片文件名_hash": {
    "liked": true,
    "liked_at": "2026-07-05T12:00:00",
    "ip": "192.168.2.x"  // 简单记录哪个设备点的
  }
}
```

## 8. 界面参考

主要参考 **拍立享** (live.pailixiang.com) 的交互：
- 首页：照片网格（5列），顶部有"照片直播"、"时间轴"、"热门"
- 大图：黑色背景，底部缩略图工具栏，左右箭头翻页
- 暗色主题为主（深紫色/深灰底）
